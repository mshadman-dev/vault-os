/**
 * materials — shared material constants and factory helpers.
 *
 * Phase 9B.1 stub. Vault PBR materials will be defined here in later phases
 * so they can be shared across mesh components without re-creation.
 */

/** Placeholder colour tokens used across materials. */
export const MATERIAL_TOKENS = {
  vaultBaseColor: '#1a1a2e',
  vaultMetalColor: '#c0c0c0',
  accentColor: '#007aff',
  emissiveAccent: '#0a4fff',
} as const
